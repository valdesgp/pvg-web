<!-- Main Navigation Wrapper -->
<nav>
  <div class="container">
    <div class="row">

      <!-- Loop through first 11 project items -->
      <% for (var i = 0; i < 11; i++) { %>

        <!-- Image Table for Each Project -->
        <table class="rounded-xl w-full text-sm text-left">
          <tr class="font-semibold grid grid-cols-1 list-inside">

            <!-- Image Grid for Project Thumbnails -->
            <div id="imageGrid" class="grid grid-cols-2 mt-3 gap-3 auto-rows-auto grid-flow-row-dense">

              <% for (var key in projects[i].gallery) { %>
                <% if (projects[i].gallery[key] && projects[i].gallery[key].link) { %>
                  <div class="list-none break-inside-avoid rounded-xl overflow-hidden">
                    <img
                      class="w-full object-cover transition-transform duration-200 hover:scale-105"
                      src="<%= projects[i].gallery[key].link %>"
                      loading="lazy"
                      alt=""
                    />
                  </div>
                <% } %>
              <% } %>

            </div> <!-- End of Image Grid -->

          </tr>
        </table>

      <% } %> <!-- End of Project Loop -->

    </div>
  </div>
</nav>

